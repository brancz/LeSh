<%= form_for @link, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :uri, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :uri, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :short_route, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :short_route, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :permanent, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :permanent, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                links_path, :class => 'btn' %>
  </div>
<% end %>
